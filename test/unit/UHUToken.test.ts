import { expect } from "chai";
import { ethers } from "hardhat";

describe("UHUToken Unit Test", function () {
  it("Should deploy with 10B supply and correct owner", async function () {
    // โค้ดทดสอบจะเริ่มตรงนี้ใน Step ถัดไป
  });
});
